import mongoose from "mongoose";

const connectDB = async()=>{
    try {
        
    } catch (error) {
        console.error('successfully connected to mongoDB👍')
        process.exit(1) //if we have some sort of error 
    }
}